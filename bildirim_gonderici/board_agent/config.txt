# Tahta Kurulum Yapılandırması
# Bu dosyayı kur.sh ile aynı dizinde (USB kökünde) bırakın.
# Tüm tahtalarda aynı USB kullanılacağından burayı bir kez doldurun.

# Okul sunucusunun (Django uygulamasının çalıştığı) IP adresi
SUNUCU_IP=192.168.1.10

# Gizli anahtar — Django settings.py'deki BILDIRIM_ANAHTAR ile aynı olmalı
GIZLI_ANAHTAR=
